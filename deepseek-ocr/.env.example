########################################
# DeepSeek OCR Service Configuration
########################################

# Model Configuration
# For MPS (Apple Silicon) support, use: Dogacel/DeepSeek-OCR-Metal-MPS
# For CUDA/original model, use: deepseek-ai/DeepSeek-OCR
MODEL_NAME=deepseek-ai/DeepSeek-OCR
HF_HOME=/models

# API Configuration
API_HOST=0.0.0.0
API_PORT=8200

# CORS Configuration (comma-separated list or * for all)
ALLOWED_ORIGINS=*

# Upload Limits
MAX_UPLOAD_SIZE_MB=100

# Device configuration (auto-detects if not set)
# Options: cuda, mps, cpu
# Leave commented to auto-detect: cuda -> mps -> cpu
# DEVICE=cuda
